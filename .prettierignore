dist/
dist-lib/
coverage/
package-lock.json

# Prose is written to be read in a diff, and a formatter that reflows a
# paragraph turns a one-word change into a rewritten page. Markdown and the
# hand-written HTML page are formatted by whoever edits them.
*.md
public/*.html

# Generated for Script Lab, and asserted by test/probe.test.ts.
probe/

# Answer sheets are recordings of what a real PowerPoint said. Reformatting one
# is editing evidence, and it fails CI for anyone who saves a sheet and commits
# it without running the formatter afterwards.
docs/host-answers/
