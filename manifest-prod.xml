<?xml version="1.0" encoding="UTF-8"?>
<OfficeApp
  xmlns="http://schemas.microsoft.com/office/appforoffice/1.1"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xmlns:bt="http://schemas.microsoft.com/office/officeappbasictypes/1.0"
  xmlns:ov="http://schemas.microsoft.com/office/taskpaneappversionoverrides"
  xsi:type="TaskPaneApp">

  <Id>43ebbbac-44ad-42b2-a582-0ef079093e6c</Id>
  <Version>1.0.0.0</Version>
  <ProviderName>StruktureretSundFornuft</ProviderName>
  <DefaultLocale>en-US</DefaultLocale>
  <DisplayName DefaultValue="SSF Merge" />
  <Description DefaultValue="Mail merge for PowerPoint. Mark a block of slides as your template, attach a table, and get one set of slides per row with every font, colour and placement intact." />
  <IconUrl DefaultValue="https://ssf-merge.struktureretsundfornuft.dk/assets/icon-32.png" />
  <!-- 64, not 80. For a TASK PANE add-in the store's icon must be 32x32 and its
       high-resolution partner 64x64; 128 is the Outlook size and 80 is the
       RIBBON size, which is a different image in a different element below.
       This said 80 and would have been caught by AppSource validation rather
       than by anyone here — the ribbon uses 80 correctly a few lines down, so
       the wrong number looked like the right one. -->
  <HighResolutionIconUrl DefaultValue="https://ssf-merge.struktureretsundfornuft.dk/assets/icon-64.png" />
  <SupportUrl DefaultValue="https://ssf-merge.struktureretsundfornuft.dk/support.html" />

  <Hosts>
    <Host Name="Presentation" />
  </Hosts>

  <!-- No <Requirements>. The floor is PowerPointApi 1.2 and it is checked at
       runtime by checkFloor, because a DECLARED set the host lacks makes the
       add-in vanish from the ribbon with no diagnostic at all. -->

  <DefaultSettings>
    <SourceLocation DefaultValue="https://ssf-merge.struktureretsundfornuft.dk/taskpane.html" />
  </DefaultSettings>

  <Permissions>ReadWriteDocument</Permissions>

  <VersionOverrides xmlns="http://schemas.microsoft.com/office/taskpaneappversionoverrides" xsi:type="VersionOverridesV1_0">
    <Hosts>
      <Host xsi:type="Presentation">
        <DesktopFormFactor>
          <GetStarted>
            <Title resid="GetStarted.Title" />
            <Description resid="GetStarted.Description" />
            <LearnMoreUrl resid="Support.Url" />
          </GetStarted>
          <ExtensionPoint xsi:type="PrimaryCommandSurface">
            <OfficeTab id="TabHome">
              <Group id="SsfMerge.Group">
                <Label resid="Group.Label" />
                <Icon>
                  <bt:Image size="16" resid="Icon.16" />
                  <bt:Image size="32" resid="Icon.32" />
                  <bt:Image size="80" resid="Icon.80" />
                </Icon>
                <Control xsi:type="Button" id="SsfMerge.OpenPane">
                  <Label resid="OpenPane.Label" />
                  <Supertip>
                    <Title resid="OpenPane.Label" />
                    <Description resid="OpenPane.Tooltip" />
                  </Supertip>
                  <Icon>
                    <bt:Image size="16" resid="Icon.16" />
                    <bt:Image size="32" resid="Icon.32" />
                    <bt:Image size="80" resid="Icon.80" />
                  </Icon>
                  <Action xsi:type="ShowTaskpane">
                    <TaskpaneId>SsfMergePane</TaskpaneId>
                    <SourceLocation resid="Taskpane.Url" />
                  </Action>
                </Control>
              </Group>
            </OfficeTab>
          </ExtensionPoint>
        </DesktopFormFactor>
      </Host>
    </Hosts>
    <Resources>
      <bt:Images>
        <bt:Image id="Icon.16" DefaultValue="https://ssf-merge.struktureretsundfornuft.dk/assets/icon-16.png" />
        <bt:Image id="Icon.32" DefaultValue="https://ssf-merge.struktureretsundfornuft.dk/assets/icon-32.png" />
        <bt:Image id="Icon.80" DefaultValue="https://ssf-merge.struktureretsundfornuft.dk/assets/icon-80.png" />
      </bt:Images>
      <bt:Urls>
        <bt:Url id="Taskpane.Url" DefaultValue="https://ssf-merge.struktureretsundfornuft.dk/taskpane.html" />
        <bt:Url id="Support.Url" DefaultValue="https://ssf-merge.struktureretsundfornuft.dk/support.html" />
      </bt:Urls>
      <bt:ShortStrings>
        <bt:String id="Group.Label" DefaultValue="SSF Merge" />
        <bt:String id="OpenPane.Label" DefaultValue="Mail merge" />
        <bt:String id="GetStarted.Title" DefaultValue="SSF Merge is ready" />
      </bt:ShortStrings>
      <bt:LongStrings>
        <bt:String id="OpenPane.Tooltip" DefaultValue="Open SSF Merge to mark a template block and merge your data into it." />
        <bt:String id="GetStarted.Description" DefaultValue="Find SSF Merge on the Home tab and mark the slides that repeat." />
      </bt:LongStrings>
    </Resources>
  </VersionOverrides>
</OfficeApp>
